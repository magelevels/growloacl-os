{
  "name": "growlocal-os",
  "version": "11.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "node --check src/index.js && node --check src/validation.js && node --check src/sales.js && node --check public/admin/admin.js && node --check public/admin/proposal.js && node --check public/audit.js && node --check public/app/app.js && node --check public/motion.js && node --test",
    "types": "wrangler types",
    "deploy:dry": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
